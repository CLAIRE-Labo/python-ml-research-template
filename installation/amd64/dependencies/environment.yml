name: <project-name>
channels:
  - conda-forge
dependencies:
  - python=<python-version>
  - pip
  - pip:
      - tqdm  # for demonstration of how to install pip packages. TQDM has no dependencies.
prefix: /opt/conda/envs/<project-name>