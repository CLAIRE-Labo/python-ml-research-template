name: <project-name>
channels:
  - conda-forge
dependencies:
  - python=<python-version>
  - pip
  - pip:
      - hydra-core
      - tqdm
      - wandb
      - pre-commit
prefix: /opt/conda/envs/<project-name>
