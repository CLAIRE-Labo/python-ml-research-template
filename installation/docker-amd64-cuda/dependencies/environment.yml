name: template-project-name
channels:
  - conda-forge
dependencies:
  - python=3.8
  - pip
  - pip:
      - hydra-core
      - tqdm
      - wandb
      - pre-commit
      - black
prefix: /opt/conda/envs/template-project-name
