# `apt` build requirements file.
# These dependencies are only installed to build the dependencies of the project.
# They will not be available in the final images running your code.
# ca-certificates     # Add if adding anything.
