name: template-project-name
channels:
  - conda-forge
dependencies:
  - python=3.10
  - pip
  - pip:
      - hatchling
      - editables
      - hydra-core
      - tqdm
      - wandb
      - pre-commit
      - black
prefix: /opt/conda/envs/template-project-name
